import React from "react";

export default function Primeiro() {

    const msg = "Seja bem vindo(a)!";

    return (
        <div>
            <h1>First New Component</h1>
            <p>{ msg }</p>
        </div>
    );
}